{
  "version": 2,
  "buildCommand": "cd .. && ls -la && if [ -d frontend ]; then cd frontend && npm install && npm run build && cd ..; fi && npm install && npm run build",
  "installCommand": "cd .. && if [ -d frontend ]; then cd frontend && npm install && cd ..; fi && npm install",
  "outputDirectory": "../frontend/dist",
  "functions": {
    "../api/index.ts": {
      "runtime": "nodejs20.x",
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
