{
  "version": 2,
  "buildCommand": "cd .. && ls -la && if [ -d frontend ]; then cd frontend && npm install && npm run build && cd ..; fi && if [ -d backend ]; then cd backend && npm install && npm run build && cd ..; fi",
  "installCommand": "cd .. && if [ -d frontend ]; then cd frontend && npm install && cd ..; fi && npm install",
  "outputDirectory": "../frontend/dist",
  "functions": {
    "../api/index.ts": {
      "runtime": "nodejs20.x",
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
