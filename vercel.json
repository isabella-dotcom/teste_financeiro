{
  "version": 2,
  "buildCommand": "cd .. && if [ -d frontend ]; then cd frontend && npm install && npm run build && cd ..; fi && if [ -d backend ]; then cd backend && npm install && npm run build && cd ..; fi",
  "installCommand": "cd .. && if [ -d frontend ]; then cd frontend && npm install && cd ..; fi && if [ -d backend ]; then cd backend && npm install && cd ..; fi",
  "functions": {
    "api/index.ts": {
      "runtime": "nodejs20.x",
      "includeFiles": "backend/dist/**,backend/node_modules/**,backend/prisma/**,backend/package.json",
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/frontend/dist/index.html"
    }
  ]
}
