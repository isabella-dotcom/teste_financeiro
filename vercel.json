{
  "version": 2,
  "installCommand": "ls -la && if [ -d \"backend\" ]; then cd backend && npm ci; else echo \"Backend directory not found!\" && exit 1; fi",
  "buildCommand": "if [ -d \"backend\" ]; then cd backend && npm run build; else echo \"Backend directory not found!\" && exit 1; fi",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "backend/**"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
