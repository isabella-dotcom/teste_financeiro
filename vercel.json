{
  "version": 2,
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm install && npm run build && cd .. && cp -r frontend/dist public; fi && if [ -d backend ]; then cd backend && npm install && npm run build && cd ..; fi",
  "installCommand": "if [ -d frontend ]; then cd frontend && npm install && cd ..; fi && if [ -d backend ]; then cd backend && npm install && cd ..; fi",
  "outputDirectory": "public",
  "functions": {
    "api/index.ts": {
      "runtime": "nodejs20.x",
      "includeFiles": "backend/dist/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
