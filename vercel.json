{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "npm run build && mkdir -p public && echo '{}' > public/.gitkeep",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
